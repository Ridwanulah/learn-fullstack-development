1. What is "conditional rendering"?

Using javascript conditino to manipulate the what react state is displayed in the DOM

2. When would you use &&?

When the first value is a boolean value or a statement that returns a boolean value

3. When would you use a ternary?

When i want to write a short if conditon

4. What if you need to decide between > 2 options on
   what to display?

Depend an how readable the code is, but most of the time 2 option conditions should use an if-else statement
